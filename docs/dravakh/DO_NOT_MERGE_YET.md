# Review gate

Do not merge `dravakh-studio-v1` into `master` until:

- build is verified;
- runtime branding change is reviewed;
- Pages deployment is confirmed or explicitly deferred;
- the fork still loads and saves standard Azgaar `.map` files.
