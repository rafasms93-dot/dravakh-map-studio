# Dravakh Map Studio deployment

The fork contains a GitHub Pages workflow prepared on branch `dravakh-studio-v1`.

Expected site path:

`https://rafasms93-dot.github.io/dravakh-map-studio/`

If no workflow run appears after branch pushes, GitHub Actions / Pages may need to be enabled once in the repository settings. Do not merge the Dravakh branch into `master` only to force a deployment; keep `master` clean until the fork changes are reviewed.
