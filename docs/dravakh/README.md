# Dravakh cartography documentation

Authoritative project-specific documents in this fork:

- `PROJECT_STATE.md` — current approved cartographic state and lost Work-session record;
- `BACKUP_PROTOCOL.md` — mandatory durable `.map` backup rules;
- `ROADMAP.md` — staged migration from generic Azgaar fork to Dravakh Map Studio.

Canonical runtime data lives in `src/data/dravakh-project.ts`.

The upstream Azgaar documentation remains available elsewhere in `docs/` and should be treated as engine documentation, not as Dravakh worldbuilding authority.
